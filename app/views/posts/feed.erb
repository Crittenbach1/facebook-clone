<% Post.all.each do |post| %>
   <%  @user = User.find(post.user_id) %>
  <%= "#{@user.first_name} #{@user.last_name}" %> <br>
   <%= "#{post.content}" %>
   <p>-------------------------------</p>
<%end%>
